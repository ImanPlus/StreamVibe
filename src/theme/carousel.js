const customTheme = {
  carousel: {
    root: {
      leftControl:
        "absolute left-0 top-0 mt-2 flex items-center justify-center px-4 focus:outline-none",
      rightControl:
        "absolute right-0 top-0 mt-2 flex items-center justify-center px-4 focus:outline-none",
    },
  },
};

export default customTheme;
